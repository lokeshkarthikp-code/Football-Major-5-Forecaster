# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# Streamlit
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db

# Data — see note in README.
# matches.parquet is small (~4k rows) and IS committed so the deployed
# app has data and the Wednesday Action can diff it. Everything else is
# derived and stays local.
# Raw CSV cache -- re-downloadable, no need to commit
data/*.csv
data/backtest_*.parquet
data/*.pkl

# API credentials — NEVER commit
.env
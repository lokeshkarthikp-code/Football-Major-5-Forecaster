pandas>=2.0
numpy>=1.24
scipy>=1.11
# soccerdata removed -- it pulled in seleniumbase/selenium (a browser stack)
# purely to fetch plain CSV files. plfc/fdcouk.py reads them with requests.
# No scraping dependency remains in this project.
streamlit>=1.30
pyarrow>=14.0
requests>=2.31